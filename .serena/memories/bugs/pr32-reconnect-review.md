PR #32 review fixes (2026-08-21): reconnect recovery previously fetched only the newest 50 messages, leaving a permanent gap after longer outages. Added reconcileReconnectHistory to walk next_cursor pages until they overlap IDs held before recovery; each page is still applied via ChatRoom.mergeJoinGap so dedupe and pendingTranscripts invariants remain intact, and the scroll nextCursor is untouched. The connected-beacon source assertion was also formatter-dependent; it now checks semantic fragments and class membership independently. Focused Node test: 6/6 pass. Bug report: .agents/results/bugs/bug-20260821-pr32-review.md.